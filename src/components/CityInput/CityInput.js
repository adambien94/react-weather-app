import React from "react";
import classes from "./CityInput.css";

const CityInput = () => {
  return <input type="text" placeholder="enter city.." className="CityInput" />;
};

export default CityInput;
